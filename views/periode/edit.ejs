<% layout('layouts/app') %>
<section  class="sectionForm">

<div class="form">

    <!-- <form action="/periode/<%= periode._id %>?_method=put" method="post" novalidate enctype="multipart/form-data"> 
        <label for="name">name</label>
        <input type="text" name="periode[name]" id="name" value="<%= periode.name %>">
        <label for="image">image</label>
        <input type="file" name="image" accept="image/*" required>
        <button type="submit">submit</button>
    </form> -->
    <form action="/periode/<%= periode._id %>?_method=put" method="post" enctype="multipart/form-data">
        <div class="title">Welcome</div>
        <div class="subtitle">Let's edit this periode!</div>
        <div class="input-container ic1">
          <input type="text" name="periode[name]" id="name" class="input" value="<%= periode.name %>" required>
          <div class="cut"></div>
          <label for="name" class="placeholder">Name of periode</label>
        </div>
        <div class="input-container ic2">
          <input type="file" name="image" accept="image/*" required>
          <label for="image" class="placeholder">Image of periode</label>
        </div>
        <button type="text" class="submit">submit</button>
    </form>
</div>
</section>